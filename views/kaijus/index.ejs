<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/kaiju.css">
    <link href="https://fonts.googleapis.com/css?family=Mukta&display=swap" rel="stylesheet">
    <title>Document</title>
</head>
<body>

    <header>
        <h1>All Known Kaiju</h1>
        <nav>
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/kaijus/new">Add New Kaiju</a>
                </li>
                <li>
                    <a href="">My Profile</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <ul>
            <% for(let i = 0; i < kaijus.length; i++) { %>
            <li>
                <button class=btn btn-primary' onlick='window.location.href='/kaijus/<%= kaijus[i]._id %>''><%= kaijus[i].name %></button>
                
                <img src="<%= kaijus[i].image %>">
                <form action="/kaijus/<%= kaijus[i]._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                </form>
                
                <button class="btn btn-primary" onclick="window.location.href='/kaijus/<%= kaijus[i]._id %>'"/edit>Edit</button>
            </li>
            <% } %>
        </ul>
    </main>
    
</body>
</html>